CONTEXT
══════════════════════════════════════════════════════════════════════════════

You are a senior QA/backend engineer running a complete validation suite
on FinRAG after five phases of SWE additions were implemented:

  Phase 1 — Docker + Docker Compose
  Phase 2 — Redis caching
  Phase 3 — PostgreSQL + SQLAlchemy + Alembic
  Phase 4 — Rate limiting + Structured logging
  Phase 5 — Async background ingestion API

The existing RAG pipeline (retrieval, reranking, citation enforcement,
LangGraph, guardrails, Langfuse) must remain completely untouched and
fully functional throughout all tests.

YOUR JOB: Run every test below in exact order. Report pass/fail for
each. If something fails, diagnose the root cause and fix it before
moving to the next test. Do not skip failures.

══════════════════════════════════════════════════════════════════════════════
PRE-FLIGHT — START FROM CLEAN STATE
══════════════════════════════════════════════════════════════════════════════

  ┌─ RUN ───────────────────────────────────────────────────────┐
  │  $ docker-compose down -v                                    │
  │  $ docker-compose up --build                                 │
  └──────────────────────────────────────────────────────────────┘

  Wait until all services are healthy before running any tests.
  
  ✅ Expected in logs:
     redis      | Ready to accept connections
     postgres   | database system is ready to accept connections
     backend    | Application startup complete
     frontend   | Ready on http://localhost:3000

  If any service fails to start: read the logs, diagnose, fix.
  Do NOT proceed until all four services are running.

  Run database migrations:
  ┌─ RUN ───────────────────────────────────────────────────────┐
  │  $ docker-compose exec backend alembic upgrade head          │
  └──────────────────────────────────────────────────────────────┘
  ✅ Expected: Running upgrade → head, no errors

══════════════════════════════════════════════════════════════════════════════
TEST BLOCK 1 — DOCKER (Phase 1)
══════════════════════════════════════════════════════════════════════════════

TEST 1.1 — All containers running
  ┌─ RUN ───────────────────────────────────────────────────────┐
  │  $ docker-compose ps                                         │
  └──────────────────────────────────────────────────────────────┘
  ✅ Expected: 4 services listed, all Status = running/healthy
  ❌ FAIL if: any service shows Exit or Restarting

TEST 1.2 — Backend reachable
  ┌─ RUN ───────────────────────────────────────────────────────┐
  │  $ curl -s http://localhost:8000/healthz | python3 -m json.tool │
  └──────────────────────────────────────────────────────────────┘
  ✅ Expected:
  {
    "status": "ok",
    "dependencies": {
      "chromadb": "ok",
      "redis": "ok",
      "postgres": "ok"
    }
  }
  ❌ FAIL if: any dependency shows "error" or request times out

TEST 1.3 — Frontend reachable
  ┌─ RUN ───────────────────────────────────────────────────────┐
  │  $ curl -s -o /dev/null -w "%{http_code}" http://localhost:3000 │
  └──────────────────────────────────────────────────────────────┘
  ✅ Expected: 200
  ❌ FAIL if: anything other than 200

TEST 1.4 — Non-root user inside backend container
  ┌─ RUN ───────────────────────────────────────────────────────┐
  │  $ docker-compose exec backend whoami                        │
  └──────────────────────────────────────────────────────────────┘
  ✅ Expected: finrag (not root)
  ❌ FAIL if: root

TEST 1.5 — .env not baked into image
  ┌─ RUN ───────────────────────────────────────────────────────┐
  │  $ docker-compose exec backend ls -la | grep "\.env$"        │
  └──────────────────────────────────────────────────────────────┘
  ✅ Expected: no .env file listed inside the container
  ❌ FAIL if: .env appears (secrets baked into image)

TEST 1.6 — Existing RAG pipeline still works end-to-end
  ┌─ RUN ───────────────────────────────────────────────────────┐
  │  $ curl -s -X POST http://localhost:8000/query \             │
  │    -H "Content-Type: application/json" \                     │
  │    -H "X-API-Key: $FINRAG_API_KEY" \                         │
  │    -d '{"query":"What was Apple total revenue?",             │
  │         "filters":{"ticker":"AAPL","filing_type":"10-K"}}' \ │
  │    | python3 -m json.tool                                    │
  └──────────────────────────────────────────────────────────────┘
  ✅ Expected:
     - "answer" field is non-empty
     - "citations" array has at least 1 item
     - "confidence" is a float between 0 and 1
     - "declined" is false
     - No 500 error
  ❌ FAIL if: empty answer, 500 error, or citations missing

BLOCK 1 RESULT: ___/6 tests passed

══════════════════════════════════════════════════════════════════════════════
TEST BLOCK 2 — REDIS CACHING (Phase 2)
══════════════════════════════════════════════════════════════════════════════

TEST 2.1 — First query is a cache MISS
  ┌─ RUN ───────────────────────────────────────────────────────┐
  │  $ curl -s -D - -X POST http://localhost:8000/query \        │
  │    -H "Content-Type: application/json" \                     │
  │    -H "X-API-Key: $FINRAG_API_KEY" \                         │
  │    -d '{"query":"What was Microsoft net income FY2025?",     │
  │         "filters":{"ticker":"MSFT","filing_type":"10-K"}}' \ │
  │    -o /tmp/query_response.json                               │
  └──────────────────────────────────────────────────────────────┘
  ✅ Expected in response HEADERS: X-Cache: MISS
  ✅ Expected in response BODY: "cached": false

TEST 2.2 — Second identical query is a cache HIT
  ┌─ RUN (same command again) ──────────────────────────────────┐
  │  $ curl -s -D - -X POST http://localhost:8000/query \        │
  │    -H "Content-Type: application/json" \                     │
  │    -H "X-API-Key: $FINRAG_API_KEY" \                         │
  │    -d '{"query":"What was Microsoft net income FY2025?",     │
  │         "filters":{"ticker":"MSFT","filing_type":"10-K"}}' \ │
  │    -o /tmp/cached_response.json                              │
  └──────────────────────────────────────────────────────────────┘
  ✅ Expected in HEADERS: X-Cache: HIT
  ✅ Expected in BODY: "cached": true

TEST 2.3 — Cache hit is significantly faster
  ┌─ RUN ───────────────────────────────────────────────────────┐
  │  $ time curl -s -X POST http://localhost:8000/query \        │
  │    -H "Content-Type: application/json" \                     │
  │    -H "X-API-Key: $FINRAG_API_KEY" \                         │
  │    -d '{"query":"What was Microsoft net income FY2025?",     │
  │         "filters":{"ticker":"MSFT","filing_type":"10-K"}}' \ │
  │    > /dev/null                                               │
  └──────────────────────────────────────────────────────────────┘
  ✅ Expected: real time < 0.5s (cached response)
  ❌ FAIL if: takes > 1s (not actually hitting cache)

TEST 2.4 — Different query is a cache MISS (not sharing cache)
  ┌─ RUN ───────────────────────────────────────────────────────┐
  │  $ curl -s -D - -X POST http://localhost:8000/query \        │
  │    -H "Content-Type: application/json" \                     │
  │    -H "X-API-Key: $FINRAG_API_KEY" \                         │
  │    -d '{"query":"What were Tesla risk factors?",             │
  │         "filters":{"ticker":"TSLA","filing_type":"10-K"}}' \ │
  │    > /dev/null                                               │
  └──────────────────────────────────────────────────────────────┘
  ✅ Expected in HEADERS: X-Cache: MISS
  ❌ FAIL if: X-Cache: HIT (wrong — this is a different query)

TEST 2.5 — Redis failure does NOT break queries
  ┌─ RUN ───────────────────────────────────────────────────────┐
  │  $ docker-compose stop redis                                 │
  │  $ curl -s -X POST http://localhost:8000/query \             │
  │    -H "Content-Type: application/json" \                     │
  │    -H "X-API-Key: $FINRAG_API_KEY" \                         │
  │    -d '{"query":"What was Apple revenue?",                   │
  │         "filters":{"ticker":"AAPL","filing_type":"10-K"}}' \ │
  │    | python3 -c "import sys,json; r=json.load(sys.stdin); print('OK' if r.get('answer') else 'FAIL')" │
  └──────────────────────────────────────────────────────────────┘
  ✅ Expected: OK (query still works without Redis)
  ❌ FAIL if: 500 error or empty response

  Restart Redis after this test:
  ┌─ RUN ───────────────────────────────────────────────────────┐
  │  $ docker-compose start redis                                │
  └──────────────────────────────────────────────────────────────┘

TEST 2.6 — Cache keys are namespaced correctly
  ┌─ RUN ───────────────────────────────────────────────────────┐
  │  $ docker-compose exec redis redis-cli KEYS "finrag:query:*" │
  └──────────────────────────────────────────────────────────────┘
  ✅ Expected: at least 2 keys listed with finrag:query: prefix
  ❌ FAIL if: keys have no prefix or KEYS returns empty

BLOCK 2 RESULT: ___/6 tests passed

══════════════════════════════════════════════════════════════════════════════
TEST BLOCK 3 — POSTGRESQL (Phase 3)
══════════════════════════════════════════════════════════════════════════════

TEST 3.1 — Tables exist with correct schema
  ┌─ RUN ───────────────────────────────────────────────────────┐
  │  $ docker-compose exec postgres psql -U finrag_user -d finrag \  │
  │    -c "\dt"                                                  │
  └──────────────────────────────────────────────────────────────┘
  ✅ Expected: query_logs and sessions tables listed
  ❌ FAIL if: either table missing

  ┌─ RUN ───────────────────────────────────────────────────────┐
  │  $ docker-compose exec postgres psql -U finrag_user -d finrag \  │
  │    -c "\d query_logs"                                        │
  └──────────────────────────────────────────────────────────────┘
  ✅ Expected: all columns present — id, api_key_hash, query_text,
     ticker, filing_type, fiscal_period, confidence_score,
     response_time_ms, was_declined, cache_hit, created_at

TEST 3.2 — Query logs are written after each request
  Note count before:
  ┌─ RUN ───────────────────────────────────────────────────────┐
  │  $ docker-compose exec postgres psql -U finrag_user -d finrag \  │
  │    -c "SELECT COUNT(*) FROM query_logs;"                     │
  └──────────────────────────────────────────────────────────────┘

  Run a new query:
  ┌─ RUN ───────────────────────────────────────────────────────┐
  │  $ curl -s -X POST http://localhost:8000/query \             │
  │    -H "Content-Type: application/json" \                     │
  │    -H "X-API-Key: $FINRAG_API_KEY" \                         │
  │    -d '{"query":"What were Googles operating expenses?",     │
  │         "filters":{"ticker":"GOOGL","filing_type":"10-K"}}' │
  │    > /dev/null && sleep 2                                    │
  └──────────────────────────────────────────────────────────────┘

  Check count after (sleep 2 gives background task time to write):
  ┌─ RUN ───────────────────────────────────────────────────────┐
  │  $ docker-compose exec postgres psql -U finrag_user -d finrag \  │
  │    -c "SELECT COUNT(*) FROM query_logs;"                     │
  └──────────────────────────────────────────────────────────────┘
  ✅ Expected: count increased by 1
  ❌ FAIL if: count unchanged

TEST 3.3 — API keys are stored as hashes (not plaintext)
  ┌─ RUN ───────────────────────────────────────────────────────┐
  │  $ docker-compose exec postgres psql -U finrag_user -d finrag \  │
  │    -c "SELECT api_key_hash FROM query_logs LIMIT 1;"         │
  └──────────────────────────────────────────────────────────────┘
  ✅ Expected: a 64-character hex string (SHA256 hash)
  ❌ FAIL if: actual API key value appears in plaintext

TEST 3.4 — Analytics endpoint returns correct data
  ┌─ RUN ───────────────────────────────────────────────────────┐
  │  $ curl -s http://localhost:8000/analytics/queries \         │
  │    -H "X-API-Key: $FINRAG_API_KEY" \                         │
  │    | python3 -m json.tool                                    │
  └──────────────────────────────────────────────────────────────┘
  ✅ Expected:
  {
    "period_days": 7,
    "total_queries": [number > 0],
    "cache_hit_rate": [float 0-1],
    "avg_response_time_ms": [number > 0],
    "decline_rate": [float 0-1],
    "top_tickers": [array with at least 1 item]
  }
  ❌ FAIL if: total_queries is 0 or endpoint returns error

TEST 3.5 — Sessions table is updated
  ┌─ RUN ───────────────────────────────────────────────────────┐
  │  $ docker-compose exec postgres psql -U finrag_user -d finrag \  │
  │    -c "SELECT api_key_hash, total_queries, last_seen_at FROM sessions;" │
  └──────────────────────────────────────────────────────────────┘
  ✅ Expected: at least 1 row, total_queries > 0
  ❌ FAIL if: table is empty

TEST 3.6 — DB write failure does not affect query response
  ┌─ RUN ───────────────────────────────────────────────────────┐
  │  $ docker-compose stop postgres                              │
  │  $ curl -s -X POST http://localhost:8000/query \             │
  │    -H "Content-Type: application/json" \                     │
  │    -H "X-API-Key: $FINRAG_API_KEY" \                         │
  │    -d '{"query":"Apple revenue test",                        │
  │         "filters":{"ticker":"AAPL","filing_type":"10-K"}}' \ │
  │    | python3 -c "import sys,json; r=json.load(sys.stdin); print('OK' if r.get('answer') else 'FAIL')" │
  └──────────────────────────────────────────────────────────────┘
  ✅ Expected: OK (query works even with postgres down)
  ❌ FAIL if: 500 error

  Restart postgres:
  ┌─ RUN ───────────────────────────────────────────────────────┐
  │  $ docker-compose start postgres                             │
  └──────────────────────────────────────────────────────────────┘

BLOCK 3 RESULT: ___/6 tests passed

══════════════════════════════════════════════════════════════════════════════
TEST BLOCK 4 — RATE LIMITING + STRUCTURED LOGGING (Phase 4)
══════════════════════════════════════════════════════════════════════════════

TEST 4.1 — Requests under limit succeed
  ┌─ RUN ───────────────────────────────────────────────────────┐
  │  $ for i in {1..5}; do                                       │
  │      curl -s -o /dev/null -w "Request $i: %{http_code}\n" \ │
  │      -X POST http://localhost:8000/query \                   │
  │      -H "Content-Type: application/json" \                   │
  │      -H "X-API-Key: $FINRAG_API_KEY" \                       │
  │      -d '{"query":"test '$i'",                               │
  │           "filters":{"ticker":"AAPL"}}';                     │
  │    done                                                       │
  └──────────────────────────────────────────────────────────────┘
  ✅ Expected: All 5 return 200

TEST 4.2 — Requests over limit return 429
  ┌─ RUN ───────────────────────────────────────────────────────┐
  │  $ for i in {1..15}; do                                      │
  │      curl -s -o /dev/null -w "Request $i: %{http_code}\n" \ │
  │      -X POST http://localhost:8000/query \                   │
  │      -H "Content-Type: application/json" \                   │
  │      -H "X-API-Key: $FINRAG_API_KEY" \                       │
  │      -d '{"query":"rate limit test '$i'",                    │
  │           "filters":{"ticker":"AAPL"}}';                     │
  │    done                                                       │
  └──────────────────────────────────────────────────────────────┘
  ✅ Expected: First 10 return 200, requests 11-15 return 429
  ❌ FAIL if: never returns 429

TEST 4.3 — 429 response has structured JSON body (not HTML)
  ┌─ RUN ───────────────────────────────────────────────────────┐
  │  $ for i in {1..12}; do                                      │
  │      curl -s -X POST http://localhost:8000/query \           │
  │      -H "Content-Type: application/json" \                   │
  │      -H "X-API-Key: $FINRAG_API_KEY" \                       │
  │      -d '{"query":"test","filters":{"ticker":"AAPL"}}' \     │
  │      > /tmp/rate_resp.json; done                             │
  │  $ cat /tmp/rate_resp.json | python3 -m json.tool            │
  └──────────────────────────────────────────────────────────────┘
  ✅ Expected: valid JSON with "error" and "message" fields
  ❌ FAIL if: HTML error page or non-JSON response

TEST 4.4 — Every response has X-Request-ID header
  ┌─ RUN ───────────────────────────────────────────────────────┐
  │  $ curl -s -D - http://localhost:8000/healthz \              │
  │    | grep -i "x-request-id"                                  │
  └──────────────────────────────────────────────────────────────┘
  ✅ Expected: X-Request-ID: [8-character-id]
  ❌ FAIL if: header not present

TEST 4.5 — Request IDs appear in logs
  ┌─ RUN ───────────────────────────────────────────────────────┐
  │  $ curl -s http://localhost:8000/healthz > /dev/null         │
  │  $ docker-compose logs backend --tail=10                     │
  └──────────────────────────────────────────────────────────────┘
  ✅ Expected: log lines contain request_id field
  ❌ FAIL if: logs are unstructured print statements with no ID

TEST 4.6 — Log output is structured
  ┌─ RUN ───────────────────────────────────────────────────────┐
  │  $ docker-compose logs backend --tail=5                      │
  └──────────────────────────────────────────────────────────────┘
  ✅ Expected: each line contains key=value pairs or JSON
     Example: request_id=a3f2b1c4 event=request_completed
              status_code=200 duration_ms=3421
  ❌ FAIL if: raw unformatted print() output

BLOCK 4 RESULT: ___/6 tests passed

══════════════════════════════════════════════════════════════════════════════
TEST BLOCK 5 — ASYNC INGESTION API (Phase 5)
══════════════════════════════════════════════════════════════════════════════

TEST 5.1 — POST /ingest returns job ID immediately
  ┌─ RUN ───────────────────────────────────────────────────────┐
  │  $ time curl -s -X POST http://localhost:8000/ingest \       │
  │    -H "Content-Type: application/json" \                     │
  │    -H "X-API-Key: $FINRAG_API_KEY" \                         │
  │    -d '{"ticker":"AAPL","filing_type":"10-K",                │
  │         "fiscal_period":"FY2025"}' \                         │
  │    | python3 -m json.tool                                    │
  └──────────────────────────────────────────────────────────────┘
  ✅ Expected:
  {
    "job_id": "[uuid]",
    "status": "pending",
    "message": "Ingestion job queued",
    "status_url": "/ingest/[uuid]/status"
  }
  ✅ Expected: real time < 0.5s (returns before ingestion starts)
  ❌ FAIL if: takes > 2s (blocking — not actually async)

  Save the job_id:
  ┌─ RUN ───────────────────────────────────────────────────────┐
  │  $ JOB_ID=$(curl -s -X POST http://localhost:8000/ingest \   │
  │    -H "Content-Type: application/json" \                     │
  │    -H "X-API-Key: $FINRAG_API_KEY" \                         │
  │    -d '{"ticker":"MSFT","filing_type":"10-K",                │
  │         "fiscal_period":"FY2025"}' \                         │
  │    | python3 -c "import sys,json; print(json.load(sys.stdin)['job_id'])") │
  │  $ echo "Job ID: $JOB_ID"                                    │
  └──────────────────────────────────────────────────────────────┘

TEST 5.2 — Job status endpoint returns live progress
  ┌─ RUN ───────────────────────────────────────────────────────┐
  │  $ curl -s http://localhost:8000/ingest/$JOB_ID/status \     │
  │    -H "X-API-Key: $FINRAG_API_KEY" \                         │
  │    | python3 -m json.tool                                    │
  └──────────────────────────────────────────────────────────────┘
  ✅ Expected:
  {
    "job_id": "[matches JOB_ID]",
    "status": "pending" | "running",
    "progress": [0-100],
    "message": "[non-empty string]"
  }
  ❌ FAIL if: 404 or job_id doesn't match

TEST 5.3 — Job state persists in Redis
  ┌─ RUN ───────────────────────────────────────────────────────┐
  │  $ docker-compose exec redis redis-cli KEYS "finrag:job:*"   │
  └──────────────────────────────────────────────────────────────┘
  ✅ Expected: at least 1 key with finrag:job: prefix
  ❌ FAIL if: no job keys in Redis

TEST 5.4 — Invalid ticker is rejected
  ┌─ RUN ───────────────────────────────────────────────────────┐
  │  $ curl -s -X POST http://localhost:8000/ingest \            │
  │    -H "Content-Type: application/json" \                     │
  │    -H "X-API-Key: $FINRAG_API_KEY" \                         │
  │    -d '{"ticker":"FAKECORP","filing_type":"10-K",            │
  │         "fiscal_period":"FY2025"}' \                         │
  │    | python3 -m json.tool                                    │
  └──────────────────────────────────────────────────────────────┘
  ✅ Expected: 400 or 422 with error message
  ❌ FAIL if: 200 with job created for invalid ticker

TEST 5.5 — Job eventually reaches completed or failed state
  ┌─ RUN (poll every 5 seconds for up to 2 minutes) ────────────┐
  │  $ for i in {1..24}; do                                      │
  │      STATUS=$(curl -s \                                      │
  │        http://localhost:8000/ingest/$JOB_ID/status \         │
  │        -H "X-API-Key: $FINRAG_API_KEY" \                     │
  │        | python3 -c \                                        │
  │        "import sys,json; print(json.load(sys.stdin)['status'])") │
  │      echo "Poll $i: $STATUS";                                │
  │      if [ "$STATUS" = "completed" ] || \                     │
  │         [ "$STATUS" = "failed" ]; then break; fi;            │
  │      sleep 5;                                                │
  │    done                                                       │
  └──────────────────────────────────────────────────────────────┘
  ✅ Expected: status reaches "completed" or "failed"
     (failed is acceptable — we're testing state transitions
      not ingestion success, which depends on your data)
  ❌ FAIL if: status stays "running" forever (stuck job)

TEST 5.6 — Completed job invalidated Redis cache for that ticker
  First add a cached query for AAPL:
  ┌─ RUN ───────────────────────────────────────────────────────┐
  │  $ curl -s -X POST http://localhost:8000/query \             │
  │    -H "Content-Type: application/json" \                     │
  │    -H "X-API-Key: $FINRAG_API_KEY" \                         │
  │    -d '{"query":"Apple revenue","filters":{"ticker":"AAPL"}}' │
  │    > /dev/null                                               │
  │  $ docker-compose exec redis redis-cli KEYS "finrag:query:*" │
  │    | grep -c "."                                             │
  └──────────────────────────────────────────────────────────────┘
  Note the cache key count.

  Then trigger AAPL ingestion and wait for completion.
  Then check cache key count again — AAPL keys should be gone.
  ✅ Expected: AAPL-related cache keys removed after ingestion
  ❌ FAIL if: stale cache keys remain after re-ingestion

BLOCK 5 RESULT: ___/6 tests passed

══════════════════════════════════════════════════════════════════════════════
FINAL SCORECARD
══════════════════════════════════════════════════════════════════════════════

  Block 1 — Docker:              ___/6
  Block 2 — Redis Caching:       ___/6
  Block 3 — PostgreSQL:          ___/6
  Block 4 — Rate Limiting/Logs:  ___/6
  Block 5 — Async Ingestion:     ___/6
  ─────────────────────────────────────
  TOTAL:                         ___/30

SCORING:
  30/30 — All phases complete and production-ready
  25-29 — Strong, minor gaps to address
  20-24 — Core phases working, some features incomplete
  < 20  — Significant issues, re-examine failing phases

For every failed test:
  1. Show the actual output received
  2. Diagnose the root cause
  3. Apply the fix
  4. Re-run the failed test
  5. Confirm it now passes before moving on

══════════════════════════════════════════════════════════════════════════════
BEGIN
══════════════════════════════════════════════════════════════════════════════

Start with PRE-FLIGHT.
Confirm all 4 services are healthy before running any tests.
Run each block in order.
Report pass/fail for every individual test.
Do not skip any test.
Fix failures before proceeding to the next block.