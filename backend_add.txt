CONTEXT & GROUND RULES
══════════════════════════════════════════════════════════════════════════════

You are a senior backend engineer extending FinRAG — a fully built,
working, production-grade financial research RAG system.

The existing system is COMPLETE and WORKING. Your job is to add SWE/backend
engineering layers ON TOP of what exists. You are not here to refactor,
redesign, rewrite, or improve the AI/RAG pipeline. Touch only what each
phase explicitly tells you to touch.

ABSOLUTE RULES — NEVER VIOLATE:
  1. Do NOT modify any existing RAG pipeline logic
  2. Do NOT touch LangGraph graph, nodes, or edges
  3. Do NOT modify the retrieval, reranking, or citation enforcement code
  4. Do NOT change the frontend beyond what Phase 1 requires for Docker
  5. Do NOT refactor working code "while you're at it"
  6. Do NOT proceed to the next phase until the current phase passes
     all verification checks and I explicitly confirm
  7. For every file you modify — show the exact diff (before/after)
     Never show full file rewrites unless the file is brand new
  8. After every implementation step, provide exact terminal commands
     and expected output before moving on

BEFORE STARTING ANY PHASE:
  Read the existing codebase structure first.
  Run: $ find . -type f -name "*.py" | head -50
  Run: $ cat requirements.txt
  Run: $ cat docker-compose.yml (if exists)
  Report what you find. Confirm the structure matches expectations
  before writing a single line of code.

══════════════════════════════════════════════════════════════════════════════
PHASE 1 — DOCKER + DOCKER COMPOSE
══════════════════════════════════════════════════════════════════════════════

WHAT THIS PHASE DOES:
  Containerizes the entire project so anyone can run it with a single
  command: docker-compose up
  This is the highest-ROI addition for SWE signaling. Its absence
  is noticed immediately by any backend interviewer.

WHAT TO BUILD:

── FILE 1: Dockerfile (backend root) ────────────────────────────────────────

  Base image: python:3.11-slim (not full — slim reduces image size)
  
  Build stages — use multi-stage build:
    Stage 1 (builder): install dependencies only
    Stage 2 (runtime): copy installed packages + source code
  
  Requirements:
    - WORKDIR /app
    - Copy requirements.txt first (layer caching — dependencies
      change less often than code)
    - pip install --no-cache-dir
    - Copy source code after deps
    - Non-root user: create a user called 'finrag', run as that user
      (security best practice — never run containers as root)
    - EXPOSE 8000
    - CMD: uvicorn api.main:app --host 0.0.0.0 --port 8000
    - Health check instruction:
      HEALTHCHECK --interval=30s --timeout=10s --start-period=60s \
        CMD curl -f http://localhost:8000/healthz || exit 1

  IMPORTANT: The start-period is 60s because the reranker model
  takes time to load on first start. Without this, Docker marks
  the container unhealthy before it's ready.

── FILE 2: .dockerignore (backend root) ─────────────────────────────────────

  Include:
    __pycache__/
    *.pyc
    *.pyo
    .env
    .env.*
    !.env.example
    data/chroma/
    data/raw/
    .git/
    .github/
    tests/
    *.md
    .pytest_cache/
    htmlcov/
    .coverage

  Explanation: data/ directories excluded because ChromaDB data
  should NOT be baked into the image — it connects to ChromaDB
  Cloud at runtime via environment variables.

── FILE 3: Dockerfile.frontend (frontend root) ──────────────────────────────

  Multi-stage Next.js build:
    Stage 1 (deps): install node_modules
    Stage 2 (builder): npm run build
    Stage 3 (runner): copy .next/standalone output only
  
  Use node:18-alpine as base (lightweight)
  Non-root user: nextjs user + nodejs group (Next.js convention)
  EXPOSE 3000
  CMD: node server.js

  Check package.json first — confirm "build" script exists.
  Check next.config.js — add output: 'standalone' if not present.
  This is required for the multi-stage frontend build to work.

── FILE 4: docker-compose.yml (project root) ────────────────────────────────

  Services:

  backend:
    build: ./backend (or wherever Dockerfile lives)
    ports: "8000:8000"
    env_file: .env
    depends_on:
      redis:
        condition: service_healthy
    volumes:
      - ./data:/app/data  (for any local file needs)
    restart: unless-stopped
    healthcheck: matches Dockerfile HEALTHCHECK

  frontend:
    build: ./frontend (or wherever frontend lives)
    ports: "3000:3000"
    environment:
      - NEXT_PUBLIC_API_URL=http://backend:8000
    depends_on:
      - backend
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    ports: "6379:6379"
    volumes:
      - redis_data:/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  volumes:
    redis_data:

  NOTE: PostgreSQL will be added to this file in Phase 3.
  Leave a comment: # postgres service will be added in phase 3

── FILE 5: docker-compose.override.yml (for local dev) ──────────────────────

  Override for local development — mounts source code as volume
  so code changes reflect without rebuilding:

  backend:
    volumes:
      - ./backend:/app  (or appropriate path)
    command: uvicorn api.main:app --host 0.0.0.0 --port 8000 --reload

  This file is automatically picked up by docker-compose and merged
  with docker-compose.yml in development. In production (Render),
  only docker-compose.yml is used.

VERIFICATION:
  ┌─ RUN ───────────────────────────────────────────────────────┐
  │  $ docker-compose build                                      │
  └──────────────────────────────────────────────────────────────┘
  ✅ Expected: Both images build successfully, 0 errors

  ┌─ RUN ───────────────────────────────────────────────────────┐
  │  $ docker-compose up                                         │
  └──────────────────────────────────────────────────────────────┘
  ✅ Expected:
     redis       | Ready to accept connections
     backend     | Application startup complete
     frontend    | Ready on http://localhost:3000

  ┌─ RUN ───────────────────────────────────────────────────────┐
  │  $ curl http://localhost:8000/healthz                        │
  └──────────────────────────────────────────────────────────────┘
  ✅ Expected: {"status": "ok"}

  ┌─ RUN ───────────────────────────────────────────────────────┐
  │  $ docker-compose down                                       │
  └──────────────────────────────────────────────────────────────┘
  ✅ Expected: All containers stop cleanly

PHASE 1 COMPLETE WHEN:
  □ docker-compose up starts all services with 0 errors
  □ Frontend loads at localhost:3000
  □ Backend healthz returns ok
  □ Redis is running and healthy
  □ Existing RAG functionality still works (run one live query)
  □ .env is NOT committed (verify with git status)

══════════════════════════════════════════════════════════════════════════════
PHASE 2 — REDIS CACHING
══════════════════════════════════════════════════════════════════════════════

WHAT THIS PHASE DOES:
  Adds a Redis cache layer to the query pipeline. Identical queries
  (same question + same filters) return cached responses in ~80ms
  instead of running the full 3-5 second pipeline. This is the most
  visible performance improvement possible and creates a strong
  interview story.

  Cache key: SHA256 hash of (query + ticker + filing_type + period)
  TTL: 3600 seconds (1 hour) — configurable via env var
  Cache hit: return immediately, skip entire pipeline
  Cache miss: run pipeline, store result, return result

WHAT TO BUILD:

── FILE 1: core/cache.py (NEW FILE) ─────────────────────────────────────────

  Create a RedisCache class with these methods:

  class RedisCache:
      def __init__(self, redis_url: str, ttl: int = 3600)
      
      def _make_key(self, query: str, filters: dict) -> str:
          """
          Creates a deterministic cache key.
          Sort filter keys before hashing to handle
          {"ticker":"AAPL","filing_type":"10-K"} and
          {"filing_type":"10-K","ticker":"AAPL"} as identical.
          Use SHA256 of the serialized string.
          Prefix all keys with "finrag:query:" namespace.
          """
      
      async def get(self, query: str, filters: dict) -> dict | None:
          """
          Returns cached response dict if exists, None if miss.
          Never raises — on Redis failure, return None and let
          the pipeline run normally. Cache failure must NEVER
          break the query flow.
          """
      
      async def set(self, query: str, filters: dict,
                    response: dict) -> None:
          """
          Stores response. Serialize with json.dumps.
          Never raises — same principle as get().
          """
      
      async def invalidate_ticker(self, ticker: str) -> int:
          """
          Deletes all cached keys containing a specific ticker.
          Returns count of deleted keys.
          Used when new filings are ingested for a company.
          Uses Redis SCAN + DEL pattern (not KEYS — never use
          KEYS in production, it blocks Redis).
          """
      
      async def health_check(self) -> bool:
          """
          Pings Redis. Returns True/False.
          Used in the /healthz endpoint.
          """

  IMPORTANT DESIGN PRINCIPLE:
  The cache wraps the pipeline — it does not go inside it.
  The RAG pipeline code is untouched. The cache check happens
  at the API route level, before the pipeline is called.

── FILE 2: api/dependencies.py (NEW FILE) ───────────────────────────────────

  FastAPI dependency injection for shared resources:

  from functools import lru_cache
  import redis.asyncio as redis

  @lru_cache()
  def get_settings() -> Settings:
      return Settings()

  async def get_redis_cache() -> RedisCache:
      settings = get_settings()
      return RedisCache(
          redis_url=settings.REDIS_URL,
          ttl=settings.CACHE_TTL_SECONDS
      )

  This pattern means Redis is instantiated once and reused
  across requests — not created per request.

── FILE 3: api/routes.py (MODIFY EXISTING) ──────────────────────────────────

  Show me the current /query route handler first.
  Then add cache integration at the TOP of the handler — before
  any pipeline call:

  ADD at the start of the query handler:
    1. Generate cache key
    2. Check cache → if hit, return cached response immediately
       Add a response header: X-Cache: HIT
    3. If miss, continue to pipeline as before
    4. After pipeline returns, store in cache
       Add response header: X-Cache: MISS

  DO NOT change anything else in the route handler.
  The pipeline call is untouched.

  Cache hit response must include a field:
    "cached": true
  Cache miss response must include:
    "cached": false

── FILE 4: Update /healthz endpoint ─────────────────────────────────────────

  Find the existing /healthz endpoint.
  Extend it (do not replace it) to include Redis status:

  Current response: {"status": "ok"}

  New response:
  {
    "status": "ok",
    "dependencies": {
      "chromadb": "ok" | "error",
      "redis": "ok" | "error"
    },
    "version": "1.0.0"
  }

  If any dependency is "error", overall status becomes "degraded"
  not "error" — the service is still running, just with reduced
  capability. This is the correct production health check pattern.

── FILE 5: Add to .env.example ──────────────────────────────────────────────

  # Redis
  REDIS_URL=redis://localhost:6379/0
  CACHE_TTL_SECONDS=3600

── ADD to requirements.txt ──────────────────────────────────────────────────

  redis[asyncio]>=4.6.0

VERIFICATION:
  ┌─ RUN ───────────────────────────────────────────────────────┐
  │  $ docker-compose up                                         │
  └──────────────────────────────────────────────────────────────┘

  Test 1 — Cache miss (first query):
  ┌─ RUN ───────────────────────────────────────────────────────┐
  │  $ curl -X POST http://localhost:8000/query \                │
  │    -H "Content-Type: application/json" \                     │
  │    -H "X-API-Key: your_key" \                                │
  │    -d '{"query":"Apple revenue FY2025",                      │
  │         "filters":{"ticker":"AAPL","filing_type":"10-K"}}'   │
  └──────────────────────────────────────────────────────────────┘
  ✅ Expected: Response headers contain X-Cache: MISS
               Response body contains "cached": false
               Response time: 3-5 seconds (full pipeline)

  Test 2 — Cache hit (same query):
  ┌─ RUN ───────────────────────────────────────────────────────┐
  │  $ (same curl command again)                                 │
  └──────────────────────────────────────────────────────────────┘
  ✅ Expected: Response headers contain X-Cache: HIT
               Response body contains "cached": true
               Response time: < 200ms

  Test 3 — Health check includes Redis:
  ┌─ RUN ───────────────────────────────────────────────────────┐
  │  $ curl http://localhost:8000/healthz                        │
  └──────────────────────────────────────────────────────────────┘
  ✅ Expected: {"status":"ok","dependencies":{"redis":"ok",...}}

  Test 4 — Cache survives pipeline failure gracefully:
  Stop Redis: $ docker-compose stop redis
  Run a query — it should still work (cache miss, pipeline runs)
  ✅ Expected: Query succeeds, no 500 error, just slower

PHASE 2 COMPLETE WHEN:
  □ Cache miss runs full pipeline, stores result
  □ Cache hit returns in < 200ms
  □ X-Cache header present on all /query responses
  □ /healthz shows Redis status
  □ Redis failure does not break query functionality
  □ Existing RAG pipeline code is completely untouched

══════════════════════════════════════════════════════════════════════════════
PHASE 3 — POSTGRESQL + SQLALCHEMY + ALEMBIC
══════════════════════════════════════════════════════════════════════════════

WHAT THIS PHASE DOES:
  Adds a PostgreSQL database for structured, queryable metadata —
  query logs and session tracking. This explicitly does NOT replace
  ChromaDB. ChromaDB handles vector search. PostgreSQL handles
  relational, analytical data. Using both demonstrates you understand
  "right tool for the right job" — one of the best interview
  conversations this project creates.

WHAT TO BUILD:

── FILE 1: Add PostgreSQL to docker-compose.yml ─────────────────────────────

  Add under services (this is where the Phase 1 comment was left):

  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: finrag
      POSTGRES_USER: finrag_user
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports: "5432:5432"
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U finrag_user -d finrag"]
      interval: 10s
      timeout: 5s
      retries: 5

  Add postgres_data to the volumes section.
  Add depends_on postgres to the backend service.

── FILE 2: database/models.py (NEW FILE) ────────────────────────────────────

  from sqlalchemy import Column, String, Integer, Float,
    Boolean, DateTime, Text, Index
  from sqlalchemy.orm import DeclarativeBase
  from datetime import datetime, timezone

  class Base(DeclarativeBase):
      pass

  class QueryLog(Base):
      __tablename__ = "query_logs"

      id = Column(Integer, primary_key=True, autoincrement=True)
      
      # Identity (hashed — never store raw API keys)
      api_key_hash = Column(String(64), nullable=False, index=True)
      
      # Query details
      query_text = Column(Text, nullable=False)
      ticker = Column(String(10), nullable=True, index=True)
      filing_type = Column(String(10), nullable=True)
      fiscal_period = Column(String(20), nullable=True)
      
      # Result metadata
      confidence_score = Column(Float, nullable=True)
      response_time_ms = Column(Integer, nullable=True)
      was_declined = Column(Boolean, default=False)
      cache_hit = Column(Boolean, default=False)
      chunk_count = Column(Integer, nullable=True)
      
      # Timestamp
      created_at = Column(
          DateTime(timezone=True),
          default=lambda: datetime.now(timezone.utc),
          index=True
      )

      # Composite index for analytics queries
      __table_args__ = (
          Index('ix_query_logs_ticker_created',
                'ticker', 'created_at'),
      )

  class Session(Base):
      __tablename__ = "sessions"

      id = Column(Integer, primary_key=True, autoincrement=True)
      api_key_hash = Column(String(64), nullable=False,
                            unique=True, index=True)
      created_at = Column(
          DateTime(timezone=True),
          default=lambda: datetime.now(timezone.utc)
      )
      last_seen_at = Column(
          DateTime(timezone=True),
          default=lambda: datetime.now(timezone.utc),
          onupdate=lambda: datetime.now(timezone.utc)
      )
      total_queries = Column(Integer, default=0)

── FILE 3: database/connection.py (NEW FILE) ────────────────────────────────

  from sqlalchemy.ext.asyncio import (
      create_async_engine,
      AsyncSession,
      async_sessionmaker
  )
  from .models import Base
  import os

  DATABASE_URL = os.getenv("DATABASE_URL")

  engine = create_async_engine(
      DATABASE_URL,
      pool_size=5,          # max 5 connections (free tier friendly)
      max_overflow=10,
      pool_pre_ping=True,   # test connections before using them
      echo=False            # set True only for debugging
  )

  AsyncSessionLocal = async_sessionmaker(
      engine,
      class_=AsyncSession,
      expire_on_commit=False
  )

  async def get_db() -> AsyncSession:
      """FastAPI dependency for database sessions."""
      async with AsyncSessionLocal() as session:
          try:
              yield session
              await session.commit()
          except Exception:
              await session.rollback()
              raise

── FILE 4: database/repository.py (NEW FILE) ────────────────────────────────

  All database operations in one place. No raw SQL anywhere else.

  class QueryLogRepository:

      async def create_log(
          self,
          session: AsyncSession,
          api_key_hash: str,
          query_text: str,
          ticker: str | None,
          filing_type: str | None,
          fiscal_period: str | None,
          confidence_score: float | None,
          response_time_ms: int,
          was_declined: bool,
          cache_hit: bool,
          chunk_count: int | None
      ) -> QueryLog:
          """Insert a new query log record."""

      async def get_analytics(
          self,
          session: AsyncSession,
          ticker: str | None = None,
          days: int = 7
      ) -> dict:
          """
          Returns aggregated analytics:
          - total queries
          - cache hit rate
          - average response time
          - decline rate
          - top tickers queried
          Filters by ticker if provided.
          Filters to last N days.
          """

  class SessionRepository:

      async def upsert_session(
          self,
          session: AsyncSession,
          api_key_hash: str
      ) -> Session:
          """
          Creates session if not exists.
          Updates last_seen_at and increments total_queries
          if exists.
          Use INSERT ... ON CONFLICT DO UPDATE pattern.
          """

── FILE 5: Set up Alembic ───────────────────────────────────────────────────

  Run inside the backend directory:

  ┌─ RUN ───────────────────────────────────────────────────────┐
  │  $ alembic init alembic                                      │
  └──────────────────────────────────────────────────────────────┘

  Configure alembic/env.py:
    - Import Base from database.models
    - Set target_metadata = Base.metadata
    - Read DATABASE_URL from environment variable

  Create initial migration:
  ┌─ RUN ───────────────────────────────────────────────────────┐
  │  $ alembic revision --autogenerate -m "create query_logs and sessions tables"  │
  └──────────────────────────────────────────────────────────────┘

  Show me the generated migration file before running it.
  Verify it creates both tables with correct columns and indexes.

  Run migration:
  ┌─ RUN ───────────────────────────────────────────────────────┐
  │  $ alembic upgrade head                                      │
  └──────────────────────────────────────────────────────────────┘
  ✅ Expected: Tables created in PostgreSQL

── FILE 6: Integrate logging into api/routes.py ─────────────────────────────

  In the /query route handler, AFTER the response is generated
  (or cache hit is confirmed), add a background task to log it:

  from fastapi import BackgroundTasks
  import hashlib, time

  def hash_api_key(api_key: str) -> str:
      return hashlib.sha256(api_key.encode()).hexdigest()

  # In the route handler — add BackgroundTasks parameter
  # After response is ready:
  background_tasks.add_task(
      log_query,
      api_key_hash=hash_api_key(api_key),
      query_text=request.query,
      ticker=request.filters.get("ticker"),
      filing_type=request.filters.get("filing_type"),
      fiscal_period=request.filters.get("fiscal_period"),
      confidence_score=response.confidence,
      response_time_ms=elapsed_ms,
      was_declined=response.declined,
      cache_hit=was_cache_hit,
      chunk_count=len(response.citations)
  )

  CRITICAL: Use BackgroundTasks — never await the DB write in the
  critical path. The user gets their response immediately. The log
  is written asynchronously after. A DB write failure must NEVER
  affect the query response.

── FILE 7: Add /analytics endpoint (NEW) ────────────────────────────────────

  GET /analytics/queries

  Query params:
    ticker: optional string
    days: optional int (default 7)

  Response:
  {
    "period_days": 7,
    "total_queries": 142,
    "cache_hit_rate": 0.34,
    "avg_response_time_ms": 2840,
    "decline_rate": 0.08,
    "top_tickers": [
      {"ticker": "AAPL", "count": 67},
      {"ticker": "MSFT", "count": 41}
    ]
  }

  This endpoint requires the same API key auth as /query.
  It reads from query_logs via QueryLogRepository.get_analytics().

── FILE 8: Update /healthz ──────────────────────────────────────────────────

  Extend the health check to include PostgreSQL:

  {
    "status": "ok",
    "dependencies": {
      "chromadb": "ok",
      "redis": "ok",
      "postgres": "ok"
    },
    "version": "1.0.0"
  }

── FILE 9: Add to .env.example ──────────────────────────────────────────────

  # PostgreSQL
  DATABASE_URL=postgresql+asyncpg://finrag_user:password@localhost:5432/finrag
  POSTGRES_PASSWORD=your_secure_password_here

── ADD to requirements.txt ──────────────────────────────────────────────────

  sqlalchemy[asyncio]>=2.0.0
  asyncpg>=0.29.0
  alembic>=1.13.0

VERIFICATION:
  ┌─ RUN ───────────────────────────────────────────────────────┐
  │  $ docker-compose up                                         │
  └──────────────────────────────────────────────────────────────┘

  ┌─ RUN ───────────────────────────────────────────────────────┐
  │  $ alembic upgrade head                                      │
  └──────────────────────────────────────────────────────────────┘
  ✅ Expected: Migration runs, tables created

  Run 3 queries via the API, then:
  ┌─ RUN ───────────────────────────────────────────────────────┐
  │  $ curl http://localhost:8000/analytics/queries \            │
  │    -H "X-API-Key: your_key"                                  │
  └──────────────────────────────────────────────────────────────┘
  ✅ Expected: JSON response showing 3 total_queries

  ┌─ RUN ───────────────────────────────────────────────────────┐
  │  $ curl http://localhost:8000/healthz                        │
  └──────────────────────────────────────────────────────────────┘
  ✅ Expected: all three dependencies show "ok"

  Verify tables directly:
  ┌─ RUN ───────────────────────────────────────────────────────┐
  │  $ docker exec -it [postgres_container] psql \               │
  │    -U finrag_user -d finrag \                                 │
  │    -c "SELECT COUNT(*) FROM query_logs;"                     │
  └──────────────────────────────────────────────────────────────┘
  ✅ Expected: count matches number of queries run

PHASE 3 COMPLETE WHEN:
  □ PostgreSQL container healthy in docker-compose
  □ Both tables created via Alembic migration
  □ Every query writes a log row (background task)
  □ /analytics/queries returns correct aggregates
  □ /healthz shows postgres status
  □ DB write failure does not affect query response
  □ No raw SQL anywhere — all queries via SQLAlchemy ORM
  □ API keys stored as SHA256 hashes only

══════════════════════════════════════════════════════════════════════════════
PHASE 4 — RATE LIMITING + STRUCTURED LOGGING
══════════════════════════════════════════════════════════════════════════════

WHAT THIS PHASE DOES:
  Rate limiting: prevents abuse, shows API design awareness.
  Structured logging: replaces any print statements with JSON logs
  that include request IDs, making the system debuggable in production.
  Both are lightweight additions that signal backend maturity.

WHAT TO BUILD:

── PART A: RATE LIMITING ─────────────────────────────────────────────────────

── FILE 1: Add slowapi to requirements.txt ──────────────────────────────────

  slowapi>=0.1.9
  limits>=3.6.0

── FILE 2: Integrate slowapi into api/main.py ───────────────────────────────

  Show me the current main.py first.
  Add rate limiter — DO NOT change anything else:

  from slowapi import Limiter, _rate_limit_exceeded_handler
  from slowapi.util import get_remote_address
  from slowapi.errors import RateLimitExceeded

  limiter = Limiter(
      key_func=get_remote_address,
      default_limits=["100/hour"]
  )
  app.state.limiter = limiter
  app.add_exception_handler(RateLimitExceeded,
                             _rate_limit_exceeded_handler)

── FILE 3: Apply rate limit to /query route ─────────────────────────────────

  On the /query route handler only:

  @router.post("/query")
  @limiter.limit(
      os.getenv("RATE_LIMIT_PER_MINUTE", "10") + "/minute"
  )
  async def query(..., request: Request):
      ...

  Rate limit is environment-configurable.
  Default: 10/minute (reasonable for a demo API).
  When limit exceeded: 429 response with JSON body:
  {
    "error": "RATE_LIMIT_EXCEEDED",
    "message": "Too many requests. Limit: 10/minute.",
    "retry_after_seconds": 30
  }

── ADD to .env.example ──────────────────────────────────────────────────────

  RATE_LIMIT_PER_MINUTE=10

── PART B: STRUCTURED LOGGING ────────────────────────────────────────────────

── FILE 4: core/logging.py (NEW FILE) ───────────────────────────────────────

  import structlog
  import logging
  import sys

  def configure_logging(log_level: str = "INFO"):
      """
      Configures structlog for JSON output in production,
      human-readable output in development.
      Reads LOG_FORMAT env var: "json" or "console" (default)
      """
      processors = [
          structlog.contextvars.merge_contextvars,
          structlog.processors.add_log_level,
          structlog.processors.TimeStamper(fmt="iso"),
      ]

      if os.getenv("LOG_FORMAT") == "json":
          processors.append(structlog.processors.JSONRenderer())
      else:
          processors.append(
              structlog.dev.ConsoleRenderer(colors=True)
          )

      structlog.configure(
          processors=processors,
          wrapper_class=structlog.make_filtering_bound_logger(
              logging.getLevelName(log_level)
          ),
          context_class=dict,
          logger_factory=structlog.PrintLoggerFactory(sys.stdout)
      )

  logger = structlog.get_logger()

── FILE 5: core/middleware.py (NEW FILE) ────────────────────────────────────

  Request ID middleware — adds a unique ID to every request
  that flows through every log line:

  import uuid
  from starlette.middleware.base import BaseHTTPMiddleware
  import structlog
  import time

  class RequestIDMiddleware(BaseHTTPMiddleware):
      async def dispatch(self, request, call_next):
          request_id = str(uuid.uuid4())[:8]  # short 8-char ID
          
          # Bind request_id to structlog context
          # Every log.info() call after this automatically
          # includes request_id in the output
          structlog.contextvars.clear_contextvars()
          structlog.contextvars.bind_contextvars(
              request_id=request_id,
              path=request.url.path,
              method=request.method
          )
          
          start = time.time()
          response = await call_next(request)
          duration_ms = int((time.time() - start) * 1000)
          
          # Log every request completion
          structlog.get_logger().info(
              "request_completed",
              status_code=response.status_code,
              duration_ms=duration_ms
          )
          
          # Add request ID to response headers
          response.headers["X-Request-ID"] = request_id
          return response

── FILE 6: Add middleware to api/main.py ────────────────────────────────────

  Show current middleware configuration.
  ADD (do not replace existing middleware):

  from core.middleware import RequestIDMiddleware
  from core.logging import configure_logging

  configure_logging()
  app.add_middleware(RequestIDMiddleware)

── FILE 7: Replace print statements in existing code ────────────────────────

  Search for any print() calls in the non-RAG pipeline code:
  ┌─ RUN ───────────────────────────────────────────────────────┐
  │  $ grep -rn "print(" api/ core/ database/ --include="*.py"  │
  └──────────────────────────────────────────────────────────────┘

  Replace each one with the appropriate structlog call:
    print("Starting query") → log.info("query_started")
    print(f"Error: {e}") → log.error("query_failed", error=str(e))

  Do NOT touch print statements in the RAG pipeline files
  (retrieval/, graph/, guardrails/) — those are out of scope.

── ADD to requirements.txt ──────────────────────────────────────────────────

  structlog>=24.1.0

── ADD to .env.example ──────────────────────────────────────────────────────

  LOG_LEVEL=INFO
  LOG_FORMAT=console

VERIFICATION:
  ┌─ RUN ───────────────────────────────────────────────────────┐
  │  $ docker-compose up                                         │
  └──────────────────────────────────────────────────────────────┘

  Test rate limiting:
  ┌─ RUN ───────────────────────────────────────────────────────┐
  │  $ for i in {1..12}; do                                      │
  │      curl -s -o /dev/null -w "%{http_code}\n" \              │
  │      -X POST http://localhost:8000/query \                   │
  │      -H "X-API-Key: key" \                                   │
  │      -H "Content-Type: application/json" \                   │
  │      -d '{"query":"test","filters":{}}';                     │
  │    done                                                       │
  └──────────────────────────────────────────────────────────────┘
  ✅ Expected: First 10 return 200, 11th and 12th return 429

  Test request ID:
  ┌─ RUN ───────────────────────────────────────────────────────┐
  │  $ curl -I http://localhost:8000/healthz                     │
  └──────────────────────────────────────────────────────────────┘
  ✅ Expected: Response headers include X-Request-ID: [8-char-id]

  Test structured logs:
  ✅ Expected in docker-compose logs:
     {"request_id": "a3f2b1c4", "event": "request_completed",
      "status_code": 200, "duration_ms": 3421, "level": "info"}

PHASE 4 COMPLETE WHEN:
  □ Rate limit returns 429 after 10 requests/minute
  □ 429 response has structured JSON body (not HTML)
  □ Every response has X-Request-ID header
  □ Every request logs a completion entry with duration_ms
  □ Log output is structured (key=value or JSON format)
  □ No new print() statements in api/ or core/ modules

══════════════════════════════════════════════════════════════════════════════
PHASE 5 — ASYNC BACKGROUND INGESTION API
══════════════════════════════════════════════════════════════════════════════

WHAT THIS PHASE DOES:
  Turns the manual ingestion script into an API endpoint.
  POST /ingest kicks off ingestion in the background and returns
  a job ID immediately. GET /ingest/{job_id}/status lets you poll
  progress. This demonstrates async job patterns, a common backend
  interview topic. It does NOT change the ingestion logic itself —
  only wraps it in an async API layer.

WHAT TO BUILD:

── FILE 1: core/job_store.py (NEW FILE) ─────────────────────────────────────

  Uses Redis to store job state (reuses Phase 2 Redis connection):

  JOB STATES: "pending" | "running" | "completed" | "failed"

  class JobStore:
      KEY_PREFIX = "finrag:job:"
      JOB_TTL = 86400  # 24 hours

      async def create_job(self, ticker: str,
                           filing_type: str) -> str:
          """Creates job, returns job_id (UUID)."""

      async def update_job(self, job_id: str,
                           status: str,
                           progress: int = 0,
                           message: str = "",
                           error: str | None = None) -> None:
          """Updates job state in Redis."""

      async def get_job(self, job_id: str) -> dict | None:
          """Returns job state dict or None if not found."""

  Job state shape in Redis:
  {
    "job_id": "uuid",
    "ticker": "AAPL",
    "filing_type": "10-K",
    "status": "running",
    "progress": 45,
    "message": "Embedding chunk 45 of 100",
    "created_at": "2025-01-01T00:00:00Z",
    "completed_at": null,
    "error": null
  }

── FILE 2: api/ingest_routes.py (NEW FILE) ──────────────────────────────────

  Two endpoints:

  POST /ingest
  Request body:
  {
    "ticker": "AAPL",
    "filing_type": "10-K",
    "fiscal_period": "FY2025"
  }

  Response (immediate — does not wait for ingestion):
  {
    "job_id": "uuid",
    "status": "pending",
    "message": "Ingestion job queued",
    "status_url": "/ingest/uuid/status"
  }

  Implementation:
    1. Validate ticker is in allowed list
    2. Create job in JobStore
    3. background_tasks.add_task(run_ingestion, job_id, ...)
    4. Return job ID immediately

  ─────────────────────────────────────────────────────────────

  GET /ingest/{job_id}/status
  Response:
  {
    "job_id": "uuid",
    "ticker": "AAPL",
    "filing_type": "10-K",
    "status": "running",
    "progress": 45,
    "message": "Embedding chunk 45 of 100",
    "created_at": "2025-01-01T00:00:00Z",
    "completed_at": null,
    "error": null
  }

── FILE 3: core/ingestion_worker.py (NEW FILE) ───────────────────────────────

  The background task function that wraps the existing ingestion
  script. DO NOT rewrite the ingestion logic. Import and call it.

  async def run_ingestion(
      job_id: str,
      ticker: str,
      filing_type: str,
      fiscal_period: str,
      job_store: JobStore
  ) -> None:
      try:
          await job_store.update_job(job_id, "running",
                                     message="Starting ingestion")

          # Import existing ingestion functions
          # Call them — do not rewrite them
          # Update progress periodically via job_store.update_job()

          await job_store.update_job(job_id, "completed",
                                     progress=100,
                                     message="Ingestion complete")

          # Invalidate Redis cache for this ticker
          await cache.invalidate_ticker(ticker)

      except Exception as e:
          await job_store.update_job(job_id, "failed",
                                     error=str(e))
          logger.error("ingestion_failed",
                       job_id=job_id, ticker=ticker, error=str(e))

VERIFICATION:
  ┌─ RUN ───────────────────────────────────────────────────────┐
  │  $ curl -X POST http://localhost:8000/ingest \               │
  │    -H "X-API-Key: your_key" \                                │
  │    -H "Content-Type: application/json" \                     │
  │    -d '{"ticker":"MSFT","filing_type":"10-K",                │
  │         "fiscal_period":"FY2025"}'                           │
  └──────────────────────────────────────────────────────────────┘
  ✅ Expected: Immediate response with job_id, status: "pending"

  ┌─ RUN ───────────────────────────────────────────────────────┐
  │  $ curl http://localhost:8000/ingest/{job_id}/status \       │
  │    -H "X-API-Key: your_key"                                  │
  └──────────────────────────────────────────────────────────────┘
  ✅ Expected: status: "running", progress increasing
  ✅ After completion: status: "completed", progress: 100

PHASE 5 COMPLETE WHEN:
  □ POST /ingest returns job_id immediately (< 100ms)
  □ GET /ingest/{id}/status shows live progress
  □ Job state persists in Redis
  □ Completed job invalidates Redis query cache for that ticker
  □ Failed job stores error message in job state
  □ Existing ingestion script/logic is completely unchanged

══════════════════════════════════════════════════════════════════════════════
FINAL VERIFICATION — ALL PHASES COMPLETE
══════════════════════════════════════════════════════════════════════════════

Run the complete system from scratch:

  ┌─ RUN ───────────────────────────────────────────────────────┐
  │  $ docker-compose down -v && docker-compose up --build       │
  └──────────────────────────────────────────────────────────────┘

Walk through this checklist:

  □ docker-compose up starts all 4 services (backend, frontend,
    redis, postgres) with 0 errors
  □ alembic upgrade head runs and creates tables
  □ /healthz shows all 3 dependencies as "ok"
  □ Full RAG query works end-to-end (existing functionality intact)
  □ Second identical query returns in < 200ms (cache hit)
  □ X-Cache header present on /query responses
  □ X-Request-ID header present on all responses
  □ Query log row created in PostgreSQL after each query
  □ /analytics/queries returns correct aggregates
  □ Rate limit triggers 429 after 10 requests/minute
  □ POST /ingest creates a background job
  □ Logs are structured with request_id on every line
  □ Frontend loads and all existing UI functionality works

UPDATED STACK SUMMARY FOR RESUME:
  FastAPI · LangGraph · ChromaDB · Redis (query caching) ·
  PostgreSQL + SQLAlchemy + Alembic (query analytics) ·
  Docker + Docker Compose · Hybrid BM25 + vector retrieval ·
  Cross-encoder reranking · Langfuse observability ·
  RAGAS evaluation · CI quality gate · Rate limiting ·
  Structured logging · Async background jobs ·
  Vercel + Render deployment

══════════════════════════════════════════════════════════════════════════════
BEGIN
══════════════════════════════════════════════════════════════════════════════

Start with PHASE 1.
Read the existing project structure first.
Show me what you find before writing any code.
Build one phase completely before starting the next.
Wait for my explicit confirmation after each phase.